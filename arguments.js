// Javascript is a dynamic language 

// Declare variable set it to a nunmber, change its type ans set it to a string

// let x = 1;
// x = 'a';

function sum() {
    let total = 0;
    for (let value of arguments)
    total += value;
    return total;

    console.log(arguments);
    return a + b; // 1 + undefined
    }

    console.log(sum(1, 2, 3, 4, 5, 10));