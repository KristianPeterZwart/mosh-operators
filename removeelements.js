
const numbers = [1, 2, 3, 4];

// End
const last = numbers.pop();
console.log(numbers);
console.log(last);

// Beginnng
const first = numbers.unshift();

// Middle
numbers.splice(2, 2);
console.log(numbers);